interface:
  display_name: "VIM4 Hardware Control"
  short_description: "Minimal Khadas VIM4 LED, ADC, GPIO, PWM, I2C/OLED, SPI, UART, Func key, and fan control helper."
  default_prompt: "Use $khadas-vim4-hardware-control to write a minimal VIM4 hardware control script. Check the matching /dev/i2c-*, /dev/spidev1.0, /dev/ttyS4, or /dev/input/event2 node before I2C/SPI/UART/Func key access."
